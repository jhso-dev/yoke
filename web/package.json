{
  "name": "yoke-web",
  "private": true,
  "version": "0.0.0",
  "description": "Next.js source for the yoke governance workbench. Built to a static export and served by the CLI's node:http server — never its own server. Deps live here, not in the root manifest, so the CLI install path does not pull a web toolchain.",
  "devDependencies": {
    "@tailwindcss/postcss": "^4.3.3",
    "@types/d3-force": "3.0.10",
    "@types/react": "19.1.13",
    "@types/react-dom": "19.1.9",
    "class-variance-authority": "^0.7.1",
    "clsx": "^2.1.1",
    "d3-force": "3.0.0",
    "lucide-react": "^1.28.0",
    "next": "16.2.12",
    "postcss": "^8.5.25",
    "react": "19.1.1",
    "react-dom": "19.1.1",
    "tailwind-merge": "^3.6.0",
    "tailwindcss": "^4.3.3",
    "tw-animate-css": "^1.4.0",
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "radix-ui": "^1.6.7",
    "react-day-picker": "^10.0.1"
  }
}
