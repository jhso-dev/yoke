node_modules/
dist/

# Local configuration. `.env.example` IS committed and lists every variable yoke reads;
# a real .env holds backend passwords and the embedder key.
.env
.env.local

*.db
*.db-wal
*.db-shm
*.tgz
.DS_Store
yoke-serve.log
yoke-serve.err.log

# Next.js (web/) build output
web/.next/
web/out/
*.tsbuildinfo

# Local git worktrees (EnterWorktree puts them here). A worktree carries its own
# biome.json, which biome rejects as a nested root configuration — so `npm run lint`
# fails for anyone who has one, on a directory that is never part of a commit.
.claude/worktrees/
